// const stu = require('./students');                   //importing code   
const {students,batch,addr} = require('./students');   //importing code

// console.log(`Students list in modules module: ${stu.students} and from batches ${stu.batch} from respective places ${stu.addr}`);
console.log(`Students list in modules module: ${students} and from batches ${batch} from respective places ${addr}`);

